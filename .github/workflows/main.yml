name: Main Workflow

on:
  workflow_dispatch:

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

  call_other_workflows:
    uses: ./.github/workflows/package1_workflow1@master
